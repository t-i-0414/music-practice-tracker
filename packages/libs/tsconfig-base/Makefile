# -------------------------------------------------------------
# Setting up
# -------------------------------------------------------------
.PHONY: setup
setup:
	@echo "🔧 Setting up TypeScript config..."
	@mkdir -p .vscode && printf '{\n  "jest.enable": false\n}' > .vscode/settings.json
	@echo "✅ TypeScript config setup complete!"
