name: Renovate
on:
  schedule:
    - cron: '0 0 * * 1-5' # every weekday at 00:00 UTC (09:00 JST)
  workflow_dispatch:
    inputs:
      dryRun:
        description: 'Dry Run mode - no PRs will be created'
        type: boolean
        required: false
        default: false
      logLevel:
        description: 'Log level'
        type: choice
        required: false
        default: 'info'
        options: [info, debug, trace]
  push:
    branches: [develop, main]
    paths:
      - '.github/renovate.json'
      - '.github/renovate.json5'
      - '.github/workflows/renovate.yml'

permissions:
  contents: write
  pull-requests: write
  issues: write

concurrency:
  group: renovate
  cancel-in-progress: true

jobs:
  renovate:
    runs-on: ubuntu-latest
    timeout-minutes: 30
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Renovate cache
        uses: actions/cache@v4
        with:
          path: |
            /tmp/renovate/cache
          key: ${{ runner.os }}-renovate-${{ hashFiles('**/bun.lockb', '**/package-lock.json', '**/pnpm-lock.yaml', '**/yarn.lock') }}
          restore-keys: |
            ${{ runner.os }}-renovate-

      # （Bun キャッシュが不要ならこのステップは削除）
      - name: Setup Bun cache
        uses: actions/cache@v4
        with:
          path: ~/.bun/install/cache
          key: ${{ runner.os }}-bun-${{ hashFiles('**/bun.lockb') }}
          restore-keys: ${{ runner.os }}-bun-

      - name: Self-hosted Renovate
        uses: renovatebot/github-action@v43.0.16
        with:
          configurationFile: .github/renovate.json
          # 任意: Renovate のイメージを固定したい場合は下記を有効化
          # renovate-version: ghcr.io/renovatebot/renovate:41.155.3
          token: ${{ secrets.RENOVATE_TOKEN }}
        env:
          NODE_OPTIONS: '--max-old-space-size=3072'
          LOG_LEVEL: ${{ github.event.inputs.logLevel || 'info' }}
          RENOVATE_REPOSITORIES: ${{ github.repository }}
          RENOVATE_DRY_RUN: ${{ github.event.inputs.dryRun || 'false' }}
          RENOVATE_BINARY_SOURCE: 'install'
          RENOVATE_CACHE_DIR: /tmp/renovate/cache
          RENOVATE_REPOSITORY_CACHE: enabled
          RENOVATE_PAGINATE_ALL: 'true'
          RENOVATE_GIT_TIMEOUT: '600000'

      - name: Upload Renovate logs
        if: failure()
        uses: actions/upload-artifact@v4
        with:
          name: renovate-logs
          path: |
            renovate.log
            /tmp/renovate/cache/renovate.log
          retention-days: 5
