#!/bin/bash
# Remove Docker resources (containers + volumes) for current worktree

set -e

SCRIPT_DIR="$(cd "$(dirname "$0")" && pwd)"
PROJECT_ROOT="$(cd "$SCRIPT_DIR/../.." && pwd)"

"$PROJECT_ROOT/bin/local/clean"
